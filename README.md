# Python Assignment — Part 3
**File I/O, API Integration, and Exception Handling**

## Project Overview
This repository contains a Python project focused on data persistence, external API communication, and robust error logging.

## File Structure
- `part3_api_files.py`: The main Python script containing all task implementations.
- `python_notes.txt`: A text file generated by the script demonstrating Write/Append/Read modes.
- `error_log.txt`: A persistent log file that tracks Python exceptions and HTTP errors.

## How to Run
1. Ensure you have the `requests` library installed:
   ```bash
   pip install requests
